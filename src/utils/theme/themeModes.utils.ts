// import lightMode from './themeModeLight.utils';
// import darkMode from './themeModeDark.utils';
import themeOptions from './ThemeOptions';

const lightmode1 = themeOptions('light');
const darkmode1 = themeOptions('dark');
const modes = {
  light: lightmode1,
  dark: darkmode1,
};

export default modes;
