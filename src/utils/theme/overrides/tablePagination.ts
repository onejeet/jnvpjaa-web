// ** Type Import
import { OwnerStateThemeType } from '.';

const TablePagination = () => {
  return {
    MuiTablePagination: {
      styleOverrides: {
        root: {
          marginRight: '60px',
        },
      },
    },
  };
};

export default TablePagination;
